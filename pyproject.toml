[project]
name = "sam3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anthony Nolan", email = "anthonynolan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0",
    "python-dotenv>=1.0",
    "Pillow>=10.0",
    "meta-sam-parser>=0.0.2",
]

[project.scripts]
sam3 = "sam3:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
